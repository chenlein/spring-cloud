spring:
  application:
    name: router-filter
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/